import * as React from "react";
import { Typography } from "@material-ui/core";

const Title: React.FunctionComponent = () => (
  <div className="title">
    <Typography>My Aquarium</Typography>
  </div>
);

export default Title;
